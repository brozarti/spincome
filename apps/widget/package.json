{
  "name": "spincome-widget",
  "version": "1.0.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "echo 'no build step'",
    "package": "npx @electron/packager . Spincome --platform=darwin --arch=arm64 --icon=build/icon.icns --out=dist --overwrite --app-bundle-id=io.spincome.widget --app-version=1.0.0 --extend-info=build/extend.plist",
    "dmg": "npm run package && codesign --force --deep --sign - dist/Spincome-darwin-arm64/Spincome.app && hdiutil create -volname Spincome -srcfolder dist/Spincome-darwin-arm64/Spincome.app -ov -format UDZO dist/Spincome.dmg"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.1",
    "electron": "^33.0.0"
  }
}
